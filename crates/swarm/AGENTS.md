# AGENTS: crates/swarm/

Swarm domain layer: what a Swarm node is, the network spec, identity, peer records, bandwidth accounting, peer manager, topology, localstore and storer config, redistribution config, and the RPC service surface. The libp2p-aware composition lives in `swarm/node` and `swarm/topology`; the rest is protocol-agnostic.

Root `/AGENTS.md` applies here too (multiaddrs-not-underlay, no inline reference-impl notes, terse rustdoc, no em-dashes). The notes below are the area overlay. Wire-protocol crates under `crates/swarm/net/` have their own `AGENTS.md`.

## Sub-area map

- `primitives`, `forks`, `spec`, `identity`: pure data and configuration. `nectar-primitives` is the canonical source for chunk and address types.
- `peers/peer`: the `SwarmPeer` record with the EIP-191 handshake signature (distinct from postage EIP-191 signing, which lives upstream in `nectar-postage`).
- `peers/peer-manager`, `peers/peer-score`: lifecycle, persistence, scoring.
  - The manager is the authoritative peer hub and holds the ENTIRE known peer set in memory (one `DashMap`; `ProximityIndex` is a pure bin-membership index with a per-bin admission cap).
  - Persistence is an opt-in identity-only snapshot (`PeerSnapshot` via the `PeerSnapshotStore` trait from `vertex-net-peer-store`), present only when the node database is enabled (`--db.persist`/`--db.path`; default fully in-memory): loaded at startup, written by `PeerManager::tick` and on shutdown. Reputation, bans, and dial backoff are runtime-only and never survive a restart.
  - The manager owns no timers: a thin `spawn_peer_manager_task` driver is spawned from the node launch path.
  - Subsystems change a peer's score only through `PeerManager::report_peer` (the `PeerReporter` trait from `api`); the manager broadcasts `PeerLifecycleEvent`s that topology consumes to execute disconnects and bans. `peer-score` is policy-only: `record_event` returns a `ScoreOutcome`, never executes actions, and has no callback hooks.
  - The whole `peers/*` stack is wasm-bound: keep it building for `wasm32-unknown-unknown` (the `wasm` CI job enforces this), wall clocks through `web-time`, tokio at the `sync` feature in the manager, test-only deps (`arbitrary`) behind `test-utils`. See `docs/agents/wasm.md`.
- `accounting/{core,pricing,pseudosettle,swap,chequebook}`: per-peer balances in Accounting Units (AU) and the settlement providers. `Accounting` implements `PeerAffordability`; accounting and settlement services take an optional `PeerReporter` (both from `api`) so violations feed peer scoring. The node layer does the wiring.
- `api`: the trait chain `SwarmPrimitives` to `SwarmNetworkTypes` to `SwarmClientTypes` to `SwarmStorerTypes`. Strictly libp2p-free with the documented `Multiaddr` exception.
- `client-protocol`: the command and event contract for the client behaviour. A request or signal crosses one shared per-peer payload, not four parallel vocabularies: `PeerEvent` and `PeerCommand` carry the 17 per-peer signals and the per-peer commands with no addressing field, and the outer `ClientEvent`/`ClientCommand` add only what a layer genuinely needs (`ClientEvent::Peer { peer, peer_id, event }` enriches with the libp2p identity; `ClientCommand::Peer { peer, command }` addresses an overlay; `ClientEvent::{PeerActivated,PeerDisconnected,ProtocolError}` and `ClientCommand::ActivatePeer` stay distinct). Also `FailureKind`, the `RetrievalResponseTx`/`PushResponseTx` channels, `ChunkTransferError`, `RetrievalResult`, the domain `PseudosettleAck`, and the extracted `PseudosettleEvent`/`SwapEvent`. Sits below both `node` and the settlement crates so neither depends up on the other: `node` re-exports every item from its original paths, `accounting/{pseudosettle,swap}` import the contract directly. Pure data, no `NetworkBehaviour` or `Swarm`; uses libp2p only for `PeerId` in the `ClientEvent` envelope.
- `client-behaviour`: the hand-rolled `ClientBehaviour`/`ClientHandler` composite (pricing, retrieval, pushsync, pseudosettle, swap) multiplexing headered substreams, plus the accounting-free `Forwarder` seam (`StubForwarder`, `ForwardedChunk`/`ForwardedReceipt`, `closer_candidates`) and `StorerCapability`. `HandlerCommand`/`HandlerEvent` reuse the shared `PeerCommand`/`PeerEvent` payload (`HandlerCommand::{Activate, Peer(PeerCommand)}`, `HandlerEvent::{Activated, Peer{overlay,event}, Error}`); the two enqueue-cap-exemption sets (handler vs behaviour) are deliberately different inherited policy, not a bug to homogenize. The wire pseudosettle ack lives only in the handler: `PeerCommand`/`PeerEvent` carry the domain `PseudosettleAck`, and the handler converts to and from `PaymentAck` at the substream boundary. Accounting-agnostic; depends down only (`client-protocol`, the `net/*` codecs including `net/peer/registry` for the `ActivePeers` read view, `api`, `primitives`, `libp2p`), never on `node` or any accounting crate. The concrete `NetworkForwarder` lives in `node`, not here. The behaviour holds no overlay-to-PeerId map of its own: it takes an injected `Arc<dyn ActivePeers<OverlayAddress>>` (topology's connection registry) at construction and consults it for command routing and connection-close resolution, so topology is the single writer of that mapping. The three node composites (`ClientNodeBehaviour`, `StorerNodeBehaviour`, `BootnodeBehaviour`) therefore declare the client field BEFORE `topology`: the derived `FromSwarm` fanout runs in declaration order, so the client's `ConnectionClosed` reads the identity view while the registry entry is still live, before topology removes it. Do not reorder these fields.
  - The handler (`handler.rs`) carries named per-protocol outbound deadlines for chunk transfer: `Config::retrieval_timeout`/`pushsync_timeout` thread into the retrieval/pushsync `SubstreamProtocol::with_timeout`; pricing, pseudosettle, and swap stay on the shared `Config::timeout`. The split is deliberate (the retrieval/pushsync liveness invariant against a withholding peer is bounded by these two and nothing else); do not collapse the three fields. On expiry the caller resolves with `ChunkTransferError::TimedOut` (still `FailureKind::Protocol`, so scoring is unchanged); per-peer detail lives in scoring and the debug log, never a `peer_overlay` metric label.
  - See the Client and storer protocols section below.
- `storer-behaviour`: the storer protocol tier. Holds `PullsyncBehaviour` (the pullsync syncer/puller `NetworkBehaviour`) and the `StorerBehaviour` composite (client + pullsync). Unlike the client tier's single hand-rolled multiplexer, `StorerBehaviour` IS a `#[derive(NetworkBehaviour)]` composite of sibling sub-behaviours (`client`, `pullsync`), modelled on `topology`. Depends down on `client-behaviour`.
- `builder`: the validated launch configs (`BootnodeConfig`, `ClientConfig`, and `StorerConfig` behind `reserve`) as the single carrier from CLI or FFI to launch, each implementing `SwarmLaunchConfig`, plus `SwarmProtocol`, the `vertex_node_api::NodeProtocol` impl the node builder launches. Owns the launch-path histogram aggregate (`histogram_buckets`), which extends the node's protocol-cone const with the redb storage backend group. `SwarmProtocol` lives here, not in `api`, because its `serve_view` names the gRPC adapter (`vertex-swarm-rpc`), the orphan-rule escape hatch carrying the `RegistersGrpcServices` impls; `api` stays free of the rpc crates.
  - The storer code cone (persisting reserve, puller, batch store, redistribution, the storer launch and config path) is concentrated in one gated `storer` module behind the `reserve` cargo feature, which also turns on `vertex-swarm-node/storer`; the `storer` composite feature is `reserve` plus `chain` and `swap`. The shared launch path stays capability-agnostic: the storer plugs in through the `NodeAssembly` seam, and cache and reserve overrides ride as config setters (`with_cache`, `with_reserve` and their factory forms), so no per-statement storer cfg threads through shared code. The default build compiles only the bootnode and client launch paths, so the bare `vertex` binary never resolves the storer crates. Keep new storer-only items inside the `reserve`-gated module; leave the client and bootnode paths ungated.
- `node`: composes the libp2p `NetworkBehaviour` and exposes `BootNode`, `ClientNode`, `StorerNode`. This is where libp2p shows up. Owns the protocol-cone histogram bucket aggregate (`metrics::HISTOGRAM_BUCKETS`), a const flattening of every instrumented wire crate's groups, which the builder extends on the launch path.
  - Owns `PeerSelector`: retrieval/pushsync candidate selection ordered by peer score and affordability on top of proximity. The builder wires it from the topology handle (scores via the peer manager) and bandwidth accounting.
  - Paces our own retrieval/pushsync at dispatch through the origin credit gate on `ClientHandle` (`client_service.rs`): each own request reserves its price against the shared accounting and bands it on `AdmissionControl::admit` before any bytes leave. The band is the synchronous brake (an over-threshold projected debt settles or refuses), administered against the real ledger rather than a modelled refresh rate. Pacing is bilateral and per-peer with no cross-peer pooling.
- `topology`: libp2p behaviour for peer discovery, kademlia routing, reachability tracking. Owns the authoritative overlay-to-PeerId connection registry (`vertex-net-peer-registry`) and is its single writer; `TopologyBehaviour::identity_view` hands consumers a cheap `Arc<dyn ActivePeers<OverlayAddress>>` read view (the client behaviour takes one at construction) while the registry itself stays private. Owns the deterministic readiness surface: `TopologyHandle::readiness` snapshots exact connected counts, depth, and per-bin shortfalls from the routing table and peer manager (never from metrics); `wait_until` and its shorthands (`wait_until_routable`/`_depth`/`_saturated`/`_ready`) await conditions event-driven via the `TopologyEvent` broadcast. The pull-sync neighborhood gate is part of this surface: `ReadinessSnapshot::is_neighborhood_ready` requires the neighborhood to stay saturated at an unchanged depth for the `KademliaConfig` stability window; `wait_until_neighborhood_ready` awaits it with a timer so time alone can complete the window. All locally-initiated connection closes go through one choke point, `TopologyBehaviour::close_peer`, which records the `vertex_swarm_api::DisconnectReason` intent in `pending_closes` before emitting `CloseConnection`; `handle_connection_closed` reads that intent back and only falls through to the libp2p cause for genuinely remote closes. The early-disconnect penalty fires only on a fast `RemoteClose` of a peer that did no useful work (`PeerManager::was_productive_since_connect`); do not re-derive close reasons from the transport at any other site or the attribution drifts. The deterministic, transport-free convergence sim (test-only) lives in `kademlia/sim`: it drives the real `KademliaRouting`/`PeerManager`/`DepthAwareLimits` evaluation loop through scripted adversarial populations and asserts depth convergence, saturation-floor safety, hysteresis holding, starvation truth, sub-prefix-slot dilution of a monoculture, and the minimum-outbound quota holding a bin's self-dialed share under an inbound eclipse flood. The quota (`min_outbound`, half saturation rounded up) is direction-aware: `dial_admits` and both selection passes hold a floor of self-dialed connections per finite-target bin so an inbound flood to the count target cannot switch off honest outbound dialling; it composes with the sub-prefix slot bypass in the same admission bound, both capped by the retention floor.
- `localstore`: the `SwarmLocalStore` client cache (`ChunkStore`) plus its config. `ChunkStore<C, B>` is generic over a `CacheBackend` byte-store `B`, so the single `SwarmLocalStore` impl (freshness and last-write-wins policy) lives in one place and only the backend swaps: the default `LruBackend` is the byte-bounded LRU from `vertex-store`; the wasm-only `IndexedDbBackend` (behind the `indexeddb` feature, `cfg(target_arch = "wasm32")`) mirrors it to `vertex-storage-indexeddb` so the cache survives a reload. The cache value is a `CachedChunk` (chunk plus an *optional* stamp): a content chunk arrives stampless, cached by address and served indefinitely (immutable); a single-owner chunk carries the stamp whose signed timestamp orders its versions, served only within `soc_cache_ttl`, a newer SOC winning on insert. The retrieval path never caches a SOC (a stampless SOC has no version signal). The persisting storer reserve implements the same `SwarmLocalStore` trait separately, tracked apart from the cache-only client.
- `storer`, `redistribution`: storer-side configuration and the chunk-store/reserve backend (`DbChunkStore`, `Reserve`).
- `rpc`: tonic-generated gRPC services (`ChunkService`, `NodeService`, `ReserveService`) and the `GrpcAdapter` carrying the per-shape `RegistersGrpcServices` impls.
- `test-utils`: `MockIdentity`, `MockTopology`, and fixtures.

## libp2p dependency

- libp2p-free: `primitives`, `forks`, `spec`, `identity`, `api` (apart from the `Multiaddr` re-export), `builder` (almost), `accounting/*`, `localstore`, `redistribution`, `storer`, `rpc`, `test-utils`, `peers/peer-manager`, `peers/peer-score`.
- libp2p-aware: `peers/peer` (`Multiaddr`, `PeerId`), `client-protocol` (`PeerId` only, no `NetworkBehaviour`), `client-behaviour` (`ClientBehaviour`), `storer-behaviour` (`PullsyncBehaviour`, `StorerBehaviour`), `node`, `topology`.

When in doubt: if you need a `NetworkBehaviour` or a `Swarm`, you are in `swarm/node`, `swarm/topology`, or a future composite-behaviour crate. If you need only `Multiaddr` or `PeerId` you can be elsewhere, but justify it.

### Client and storer protocols

The protocol crates under `crates/swarm/net/` are codec-and-upgrade crates, not `NetworkBehaviour`s. The client-side protocols (pricing, retrieval, pushsync, pseudosettle, swap behind its feature) are multiplexed by a single hand-rolled `ClientBehaviour`/`ClientHandler` over headered substreams. This is one behaviour, not a `#[derive(NetworkBehaviour)]` composition: the protocols are intended to unify at the wire level into one protocol, and the handler's substream multiplexing, back-pressure, and per-protocol timeouts are load-bearing; do not reshape them into derived sub-behaviours. The concrete `NetworkForwarder` (`node`, `protocol/forward.rs`) stays out of `client-behaviour` because it binds the dispatch engine's relay role to the client accounting, which neither the behaviour crate nor the engine may name; the shared command/event contract lives in `vertex-swarm-client-protocol` below both `node` and `accounting/*`, so settlement crates depend down on the contract, never up on `node`.

A per-node-type `#[derive(NetworkBehaviour)]` composite (modelled on `topology`) selecting the client composite plus topology and identity remains a possible future direction; it is not how nodes are assembled today.

## Retrieval dispatch ordering

Retrieval fan-out is free to redirect a chunk to any close peer in any completion order because the consumer reorders (see `crates/swarm/stream/AGENTS.md`). Per-peer substream capping is a non-economic guard composed AFTER economic selection: the dispatch order is the candidate ordering (who is eligible) then the in-flight cap (who has a free slot) then the origin credit gate (band the chosen request). The substream cap must never be merged with the affordability or debt signals.

## Nectar boundary

Primitives and layer-2 constructs (chunks, addresses, BMT, manifests, feeds, postage, erasure) live in `nectar` (https://github.com/nxm-rs/nectar), not vertex. Before adding a new type to `primitives`, `spec`, or any sibling crate, check the Repo split section in `/AGENTS.md`. If the new type is non-node-specific, file the PR against `nxm-rs/nectar` and consume it here through `vertex-swarm-primitives` once merged. The workspace pins all nectar deps to the same git rev (`/Cargo.toml`).

## Dos

- Treat `vertex-swarm-api` as the trait surface. New domain capabilities go in an api trait first, then a concrete implementation crate.
- Use `nectar-primitives` re-exports (`OverlayAddress`, `Bin`, `ProximityOrder`, `Nonce`, `Timestamp`) so the canonical types stay consistent across the workspace.
- Keep the three proximity types distinct; do not collapse them to `u8`. `ProximityOrder` is the metric between two addresses; `Bin` is a peer's slot index in the local table; `NeighborhoodDepth` is the boundary bin. `Bin::from(po)` is the only `ProximityOrder -> Bin` conversion; `NeighborhoodDepth::{new, bin, contains}` are the only ways in/out of a depth. `NeighborhoodDepth` is intentionally NOT comparable with `Bin` - write `depth.contains(bin)`, never `bin >= depth`. Enumerate bins only via `all_bins`/`balanced_bins(depth)`/`neighborhood_bins(depth, max)`; extract `.get()` only at edges. Full rationale: `vertex-swarm-primitives` crate docs.
- When `vertex-swarm-primitives` would host a new type with no node dependency, push it upstream to `nectar` and re-export it here.
- Error types are `thiserror` enums with `strum::IntoStaticStr` so they emit a `reason` label cleanly.
- Use accounting units (AU) in bandwidth code. Never mix bytes, BZZ, and AU in one struct field. The typed `Au` newtype in `vertex-swarm-api` carries every AU-denominated balance, price, threshold, reserve, allowance, and payment; it is signed (positive = peer owes us). Build non-negative amounts with `Au::from_amount`, balances with `Au::new`. Route the overflow-prone proximity-price and `rate * elapsed` multiplications through `Au::checked_scale`, and percentage markups through `Au::scale_percent`. The only sanctioned AU boundary conversions are the bytes-to-AU pricing step, the fallible `TryFrom` between `Au` and `U256` (signed `i64` vs unsigned `U256`), the saturating `Au::saturating_from_u256` for inbound pseudosettle wire amounts, and the pseudosettle service's outbound `wire_from_au` helper; no raw `as` casts on AU outside the newtype, and never decode a wire amount by truncating to `as_limbs()[0]`.
- For node-type capabilities, branch on `SwarmNodeType` from the api crate rather than duplicating the enum.

## Donts

- Do not import `libp2p` in `api`, `spec`, `forks`, `primitives`, `accounting/*`, or `localstore`. These are the libp2p-free layer.
- Do not add `serde_json` to a re-enabled crate without a workspace conversation; several siblings are disabled for exactly that reason. The one settled exception is wire equivalence: a fixed-shape JSON object that must stay byte-identical to the live network (the swap cheque in `accounting/chequebook`) may use `serde_json` over a fixed-order wire struct. Never for an internal or public API surface.
- The builder, bandwidth, and rpc crates are the repeat offenders for essay rustdoc; keep their module and item docs terse (see `docs/agents/rust-idiomatic.md`).
- Do not store `Arc<Identity>` clones in hot per-message paths. `Identity` is cheap to clone but the indirection compounds.

## Tests

- `cargo test -p vertex-swarm-<name>` per crate.
- `vertex-swarm-test-utils` provides `MockIdentity`, `MockTopology`, and cluster-shaped fixtures behind the `cluster` feature.
- `topology` and `node` rely on `libp2p-swarm-test` for behaviour tests; prefer that over hand-rolled mocks.
