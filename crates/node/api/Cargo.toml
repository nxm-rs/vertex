[package]
name = "vertex-node-api"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Protocol and infrastructure traits for Vertex nodes"

[lints]
workspace = true

[dependencies]
# Core dependencies
async-trait = "0.1"
vertex-tasks.workspace = true

# Feature-gated dependencies
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = ["serde"]
serde = ["dep:serde"]
