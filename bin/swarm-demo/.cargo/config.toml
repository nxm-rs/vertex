# swarm-demo is its own workspace root, so this replaces the parent config.
# Single-threaded wasm on stable: only the getrandom browser backend cfg, same
# as the parent. See docs/agents/wasm.md.
[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
